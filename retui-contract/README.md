# Re:TUI visual contract

Vendored from the Launcher `retui-contract` module, last changed at commit
`f9aeb717693a49d52148fde421cff2893a869122`. The source and tests are copied
unchanged so keyboard builds use the real protocol keys and legacy aliases.

Keep this snapshot synchronized when the shared Launcher protocol changes.
No sibling checkout or separately published dependency is needed.
