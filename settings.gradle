pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "ReTUIKeyboard"
include ':app'
include ':retui-contract'
project(':retui-contract').projectDir = new File(settingsDir, '../../StudioProjects/TUI-ConsoleLauncher/retui-contract')
